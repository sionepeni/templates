export type DetailForm = {
    oldDevice: string,
    newDevice: string
    user: string,
    ticket: string,
    supervisor: string,
    laptop: string,
    mobile: string,
    switch: string,
}

export type TextTemplate = {
    text: string
}