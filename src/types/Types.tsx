export type BasicForm = {
    name: string,
    user: string,
    ticket: string,
    oldDevice: string,
    newDevice: string
    supervisor: string,
    laptop: string,
    mobile: string,
    switch: string,
}

export type Text = {
    text: string
}